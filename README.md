# cephutils
Utilities for the Ceph project

rjenkins.py is a Python program that computes the rjenkins hash of a string. This hash is used as a standard for the object hash in a Ceph pool.

For learning purposes the program may be used to study the output of the command: ceph osd map ..
